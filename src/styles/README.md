# Styles

Global styles and Tailwind CSS configuration.

## Structure

```
styles/
└── globals.css      # Global CSS, Tailwind directives
```

## Rules

- Use Tailwind utility classes for styling
- Avoid global CSS; prefer component-scoped styles
- CSS custom properties (variables) for design tokens — defined in `tailwind.config.ts`
